# Configurazione Supabase — copia questo file in ".env.local" e inserisci i valori reali.
# La chiave anon/publishable è pubblica (pensata per il browser): non è un segreto,
# ma .env.local NON viene committato (è nel .gitignore).

VITE_SUPABASE_URL=https://oudjuqbqszisdtwzbxdo.supabase.co
VITE_SUPABASE_ANON_KEY=incolla-qui-la-chiave-anon-publishable
