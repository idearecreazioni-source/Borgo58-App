# Credenziali degli utenti di PROVA per `npm run test:app`.
# Copiare questo file in `.env.test` e completare la password.
# `.env.test` NON va mai committato (gia' escluso da .gitignore).
# Come creare gli utenti: tests/app/LEGGIMI.md
TEST_TITOLARE_EMAIL=test-titolare@borgo58.app
TEST_TITOLARE_PASSWORD=
TEST_STAFF_EMAIL=test-staff@borgo58.app
TEST_STAFF_PASSWORD=
