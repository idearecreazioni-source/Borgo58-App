#!/bin/sh
# ---------------------------------------------------------------------
# Il commit in locale non e' piu' bloccato — 01/09/2026, deciso da Alessio
# ---------------------------------------------------------------------
# Prima questo gancio lanciava lint, le prove pure e la compilazione, e
# RIFIUTAVA il commit se qualcosa non passava. Era nato il 05/08, quando
# su GitHub non girava niente: era l'unica rete.
#
# Adesso i controlli girano su GitHub (.github/workflows/controlli.yml) e
# `master` e' protetto, quindi un commit su un ramo non pubblica niente.
# Bloccare il salvataggio del lavoro in locale non protegge piu' niente:
# ferma solo chi sta lavorando.
#
# ⚠️ E lo ferma anche quando il difetto NON e' nel suo lavoro: il 01/09 il
#    commit di Alessio e' rimasto bloccato per una riga del suo `.env`,
#    con un messaggio che diceva «qualcosa che funzionava si e' rotto»
#    mentre nessuna prova era fallita — due file non si aprivano.
#
# ⚠️ COSA NON GIRA PIU' QUI, e va saputo: i controlli sulle migrazioni
#    (§7 punti 3 e 4 — registrazione in applied_migrations, blocco di
#    verifica, rieseguibilita'). La CI oggi NON li fa: erano solo qui.
#
# Chi vuole i controlli prima di committare li lancia a mano:
#   npm run lint  ·  npm run test  ·  npm run build
exit 0
